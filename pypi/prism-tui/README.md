# prism-tui (PyPI)

Prism for the terminal, via pip. Wheels are platform-specific and bundle the
prebuilt static binary — no Go toolchain, no runtime.

```bash
pipx install prism-tui
prism-tui
```

See [tui/README.md](../../tui/README.md) for keys and behavior.
